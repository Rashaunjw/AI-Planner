# Database
# DATABASE_URL should be your pooled/pgbouncer URL (session/transaction pool)
DATABASE_URL="postgresql://username:password@localhost:5432/ai_planner"
# DIRECT_URL should be a non-pooled direct connection for Prisma migrations/build
DIRECT_URL="postgresql://username:password@localhost:5432/ai_planner"

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here"

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# OpenAI
OPENAI_API_KEY="your-openai-api-key"

# Resend
RESEND_API_KEY="your-resend-api-key"
# From address (e.g. noreply@yourdomain.com). Inbox will show sender as "PlanEra".
RESEND_FROM="noreply@aiplanner.com"

# Google Calendar API
GOOGLE_CALENDAR_API_KEY="your-google-calendar-api-key"

# Optional: Pro trial / beta code for MVP. When set, users can enter this code in Settings
# to get Pro access (unlimited uploads, schedule chat). Leave unset to disable.
# BETA_PRO_CODE="planera-beta-2025"
